:8080 {
	# API routes - proxy to backend (keep /api prefix)
	handle /api/* {
		reverse_proxy api:8000
	}

	# App routes go to frontend
	handle /app/* {
		reverse_proxy frontend:5173
	}

	# Root redirects to /app
	handle / {
		redir /app 302
	}
}
